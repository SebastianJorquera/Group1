run ```npm install``` to install all the necessary libraries

All files in the directory /public/ will be hosted.

Run ```npm test``` to start the server.

This is the link to the test code.
http://localhost:3000/public/html/test.html

Controls: wasd and space
